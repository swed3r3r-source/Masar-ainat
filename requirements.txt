# متطلبات «مسار عينات» — Python 3.11+
#
# القائمة قصيرة عمدًا. عميل PostgreSQL (بروتوكول wire الإصدار ٣ مع
# SCRAM-SHA-256) مكتوب داخل المشروع في packages/pgwire، فلا يعتمد النظام على
# psycopg ولا على أي امتداد يحتاج ترجمة. هذا يجعل النشر ممكنًا على خادم
# بلا مترجم C وبلا نفاذ إلى مستودعات الحزم.
#
#     pip install -r requirements.txt

# ── إلزامي لتشغيل الخادم ──────────────────────────────────────────────────
starlette>=0.37            # طبقة ASGI — التوجيه والطلب والاستجابة
uvicorn>=0.29              # خادم ASGI

# ── إلزامي للتشفير عند التخزين (§20) ──────────────────────────────────────
# بدونه يعود النظام إلى تنفيذ ChaCha20-Poly1305 داخلي مُتحقَّق من صحته مقابل
# متجهات RFC 8439، لكن **الإنتاج يتطلب هذه الحزمة**: تنفيذ مُدقَّق ومسرَّع
# عتاديًا أولى من تنفيذ مكتوب يدويًا مهما كان صحيحًا.
cryptography>=42.0

# ── إلزامي للاختبارات التكاملية ───────────────────────────────────────────
httpx>=0.27

# ── اختياري ───────────────────────────────────────────────────────────────
# رفع جداول Excel. بدونه يعمل رفع CSV وترفض الواجهة .xlsx برسالة واضحة
# بدل أن تفشل بغموض.
openpyxl>=3.1

# اختبار المتصفح الحقيقي (scripts/browser_walkthrough.py و scripts/ui_test.py).
# ليس لازمًا للتشغيل، ولازم لإعادة إنتاج إثبات الدورة العشرينية.
# بعد التثبيت:  python3 -m playwright install chromium
playwright>=1.44

# ── غير مثبَّت عمدًا ───────────────────────────────────────────────────────
# ortools — خلفية حلّ بديلة. المستودع يحوي **محوّلًا فقط** بلا تنفيذ حلّال
# (packages/masar_opt/backends.py). لم تُثبَّت ولم تُختبر. راجع STATUS.md.
