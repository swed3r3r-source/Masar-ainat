# حالة النسخة — ما أُثبت وما لم يُثبت

> وثيقة إقرار، لا وثيقة تسويق. كل بند هنا إما مقيس ومذكور بأرقامه، أو مذكور
> صراحةً بأنه لم يُقس. اقرأها قبل أي قرار اعتماد.

**تاريخ اللقطة:** ٢٧ أغسطس ٢٠٢٦ · **الحزمة:** ١٨١ اختبارًا · ١٨١ ناجحة ·
صفر فاشلة · **صفر متخطّاة** · ٤٦ سيناريو إلزاميًا مُثبتًا من ٤٦.

**تغييرات أمنية في هذه اللقطة:** بوابة تشفير معتمد تمنع الإقلاع خارج
development/test بلا AES-256-GCM · فحص أسرار التصدير بلا استثناء لأي ملف ·
تثبيت الاعتماديات بـ`requirements.in` وسكربت قفل بالتجزئات · حزمة نشر تجريبي
كاملة (`deploy/staging/`).

---

## ١. البندان اللذان يجب أن يُقرآ أولًا

### ١‑أ · OSRM غير متصل حيًّا في هذه النسخة

**مزوّد الطرق المُعدّ افتراضيًا هو `haversine` — وهو تقديري لا حقيقي.**
لم تُشغَّل خدمة OSRM في البيئة التي بُنيت فيها هذه النسخة: لا خدمة Docker
(`/var/run/docker.sock` غير موجود) ولا نفاذ شبكي صادر (`curl` يعيد `000`).

**ما الذي يعنيه ذلك بالأرقام** — قياس على مسار حقيقي في الحدود الشمالية
(عرعر ← رفحاء ← العويقيلة ← مختبر عرعر):

| | Haversine (تقديري) | OSRM (طرق حقيقية) | الفارق |
|---|---|---|---|
| المسافة الكلية | 759.7 كم | 593.1 كم | **+28.1٪** |
| زمن القيادة | 664 د | 406 د | **+63.7٪** |
| وقت انتهاء الرحلة | 18:29 | 15:18 | **+٣ س ١١ د** |
| ساق داخل البلدة | 2.5 كم | 12.0 كم | **−79.2٪** |

الخطأ **في اتجاهين متعاكسين**: تضخيم بين المدن، وتقليل داخل البلدة. لذلك لا
يمكن تصحيحه بمعامل واحد، ولا تصلح الخطة المبنية عليه لالتزام SLA حقيقي.

**ما أُثبت رغم ذلك:**

- **بيانات الطرق نفسها حقيقية في المقارنة أعلاه**: مصفوفة OSRM على
  OpenStreetMap جُلبت خارج نطاق الخادم وثُبِّتت في
  `var/reports/osrm-raw-northern-borders.json`. وتطابق مصادر خرائط مستقلة على
  ساق عرعر←رفحاء ضمن **+1.9٪** (290.4 كم مقابل 284.7–285)، بينما يخطئ Haversine
  بـ**+32.8٪**.
- **المحوّل مُختبَر بـ٢٧ فحصًا** مقابل خادم OSRM وهمي: بناء الرابط، ترتيب
  `lon,lat`، تحويل الوحدات، التقسيم إلى كتل، إعادة المحاولة، وكل حالات الفشل.
- **٣ اختبارات انحدار دائمة** في `tests/test_unit_rules.py`.

**ما لم يُثبت:** أن خدمة OSRM تعمل في بيئتك. ذلك ما يفحصه
`./deploy/osrm/activate.sh` و`scripts/osrm_verify.py` على خادمك.

**سلوك النظام في هذه الحالة — مقصود لا مؤقت:**

| الحالة | السلوك |
|---|---|
| مزوّد تقديري | كل خطة تحمل تحذير `ESTIMATED_TRAVEL_TIME` بشدة **HIGH**، ووسم «تقديرية» ظاهر في كل شاشة |
| اعتماد خطة تقديرية في **الإنتاج** | **مرفوض من الخادم** بـ`CONFLICT` — لا يمكن تجاوزه من الواجهة |
| اعتمادها خارج الإنتاج | يتطلب إقرارًا صريحًا `acknowledge_estimated` يُسجَّل في سجل التدقيق باسم من أقرّه |
| مزوّد حقيقي مُعدّ لكنه ساقط | التخطيط **يفشل** بـ`DEPENDENCY_UNAVAILABLE` — خطة على أزمنة مختلقة أسوأ من لا خطة |
| خدمة تستجيب من خريطة خاطئة | `preflight` يمنع الإقلاع، و`osrm_verify` يعيد رمز خروج `1` |

**للتفعيل:** `./deploy/osrm/activate.sh` — تفاصيله في `deploy/osrm/README.md`.
لا تكلفة رخص، ولا تغادر أي بيانات شبكتك.

### ١‑ب · OR-Tools غير مثبَّتة

**الموجود في المستودع محوّل (Adapter) فقط، بلا تنفيذ حلّال.**

```
$ python3 -c "import ortools"
ModuleNotFoundError: No module named 'ortools'

$ pip install ortools
ERROR: Could not find a version that satisfies the requirement ortools (from versions: none)
```

`from versions: none` يعني أن فهرس الحزم نفسه غير مقروء في تلك البيئة، لا أن
الإصدار غير متوافق. و`OrToolsBackend` في `packages/masar_opt/backends.py` يحمل
`version = "unavailable"`، و`solve()` فيه **يرمي `DependencyUnavailable`
صراحةً** — لا يعيد نتيجة مختلقة.

**الخلفية الفعالة والمختبَرة الوحيدة هي `native_alns`** (إدراج بالندم + ALNS).

**لتفعيل OR-Tools:** خادم Linux x86-64 · Python 3.9–3.12 · نفاذ إلى PyPI أو
مرآة داخلية · `pip install ortools` · `MASAR_OPTIMIZER_BACKEND=ortools`. يبقى
بعدها **عمل برمجي فعلي**: بناء `RoutingIndexManager` وبُعدَي الزمن والمسافة
وقيود `AddPickupAndDelivery`. **التثبيت وحده لا يجعلها تعمل.**

**هل هذا نقص في جودة الحل؟** لا — على الأحجام القابلة للتحقق:

| | الاستدلال (Regret‑k + ALNS) | الحل المضبوط (تفريع وتحديد) |
|---|---|---|
| المسافة | 593.15 كم | **593.15 كم** |
| زمن القيادة | 405.8 د | **405.8 د** |
| زمن الحل | ٢٠ ث (حد زمني) | ١ مل.ث · ٤٧ عقدة |
| أمثل مُثبَت | — | **نعم** |
| المدقّق المستقل | صالح · ٠ خروقات | صالح · ٠ خروقات |

**فجوة الأمثلية 0.00٪ والتسلسل مطابق حرفيًا.** أي أن OR-Tools لا يمكنها التفوق
على هذه الحالة. على مسائل وطنية بمئات الشحنات لا يوجد مرجع مضبوط، وهناك تبقى
المقارنة مفتوحة فعلًا.

---

## ٢. جدول الحالة الكامل

| المكوّن | الحالة | ما يعنيه عمليًا |
|---|---|---|
| محرّك التخطيط (PDPTW · Regret‑k + ALNS) | ✅ يعمل | فجوة أمثلية 0.00٪ مقابل حل مضبوط · ٣٠٠ شحنة في ٢٧ ث |
| الدورة التشغيلية كاملة | ✅ يعمل | ٢٠ خطوة مثبتة عبر متصفح حقيقي، من رفع الجدول إلى سجل التدقيق |
| الصلاحيات (خادم + RLS) | ✅ يعمل | ٧٤ سياسة صف · ٣٤ فحصًا هجوميًا · سجل تدقيق غير قابل للتعديل |
| التشفير عند التخزين (AES‑256‑GCM) | ✅ يعمل | مع تدوير مفاتيح · المستندات تُشفَّر عند الرفع |
| النسخ والاستعادة + أرشفة WAL | ✅ يعمل | استعادة إلى لحظة محددة · RTO يُقاس بـ`backup.sh measure` |
| التخطيط المتوازي | ✅ يعمل | ×1.93 على نواتين · نتائج مطابقة للتشغيل التسلسلي |
| الاستيراد والتحقق (CSV) | ✅ يعمل | فحص بنيوي + فحص جدوى رياضي، بسبب مصنَّف لكل صف مرفوض |
| الاستيراد (Excel) | ⚠️ اختياري | يحتاج `openpyxl`؛ بدونه ترفض الواجهة `.xlsx` برسالة واضحة |
| **أزمنة القيادة** | ⚠️ **تقديري معلن** | **+28٪ مسافة · +64٪ زمن** · اعتماده مرفوض في الإنتاج |
| الإشعارات الخارجية (SMS/بريد) | 🔌 محوّل فقط | صندوق صادر يسجّل `NO_PROVIDER` — لا يدّعي إرسالًا لم يحدث |
| حساسات الحرارة | 🔌 محوّل فقط | الحالة `NO_SENSOR` · **السائق لا يُدخل الحرارة يدويًا** |
| **OR-Tools** | 🔌 **محوّل فقط** | غير مثبَّتة · `version = "unavailable"` |
| **OSRM** | 🔌 **جاهز للتشغيل عندك** | الحزمة والتحقق مُنفَّذان · يبقى `activate.sh` على خادمك |
| خزنة الأسرار | ⏸️ قرارك | المفاتيح الآن في ملف بيئة بصلاحية `640` |
| توطين الاستضافة | ⏸️ قرارك | قرار تنظيمي قبل أي بيانات حقيقية |
| اختبار اختراق مستقل | ⏸️ قرارك | الفحوص الذاتية لا تُغني عن طرف مستقل |
| عتاد حساسات الحرارة | ⏸️ قرارك | المحوّل والتنبيهات جاهزة · ينقص العتاد |

---

## ٢أ. التشفير — بوابة الإنتاج

**AES-256-GCM عبر مكتبة `cryptography` شرط لا خيار خارج development/test.**

| البيئة | المكتبة متاحة | السلوك |
|---|---|---|
| development / test | نعم | AES-256-GCM |
| development / test | لا | التنفيذ الاحتياطي (ChaCha20-Poly1305) — مسموح هنا فقط |
| **staging / production / أي بيئة أخرى** | نعم | AES-256-GCM |
| **staging / production / أي بيئة أخرى** | **لا** | **`DependencyUnavailable` — لا إقلاع ولا تشفير ولا رجوع صامت** |

`APP_ENV` و`MASAR_ENV` يُقرآن معًا، وعند تعارضهما تُغلَّب الأشد. أدوات النشر
تضبط الأول عادةً، وقراءة أحدهما دون الآخر تجعل خادمًا يظن نفسه في التطوير وهو
في الإنتاج — فتُلغى كل البوابات بصمت.

**لماذا الرفض بدل الرجوع الصامت:** التنفيذ الاحتياطي صحيح مقابل متجهات
RFC 8439، لكن «صحيح رياضيًا» ليس «صالحًا للإنتاج»: ينقصه ما لا يُثبَت باختبار
وظيفي — مقاومة هجمات التوقيت والقنوات الجانبية، والمراجعة الخارجية، والتسريع
العتادي. والأخطر أن الرجوع إليه كان **صامتًا**.

فك تشفير حمولة احتياطية قديمة في الإنتاج ممكن لكن بقرار صريح
(`MASAR_ALLOW_LEGACY_FALLBACK_DECRYPT`) يرفض `preflight` بقاءه مفعّلًا —
مخرج ترحيل لمرة واحدة، لا ثغرة دائمة. سبعة اختبارات آلية تغطي حالة النجاح
وكل حالات الرفض.

---

## ٢ب. الاعتماديات — التثبيت والقفل

| الملف | الدور | الحالة |
|---|---|---|
| `requirements.in` | المتطلبات **المباشرة** بإصدارات مثبَّتة | موجود |
| `requirements.lock` | كل الاعتماديات بتجزئات sha256 | **غير موجود** |
| `scripts/lock_dependencies.sh` | يولّد القفل بـpip-tools | موجود |

**ملف القفل غير مُرفَق، ولم يُختلق.** توليده يتطلب اتصالًا بـPyPI، وهو محجوب
في البيئة التي بُنيت فيها هذه النسخة. التجزئة بصمة للملف المنشور ولا تُشتق من
رقم الإصدار؛ ملف قفل ملفَّق إما يفشل عند التثبيت أو — وهو الأسوأ — يمرّ
بتجزئات خاطئة فيُبطل الحماية كلها. السكربت نفسه **يرفض التوليد** بلا اتصال
ويشرح السبب.

**خطوة إلزامية على خادم متصل بـPyPI:**

```bash
./scripts/lock_dependencies.sh                       # يولّد requirements.lock
python3 -m venv .venv && . .venv/bin/activate
pip install --require-hashes -r requirements.lock    # التثبيت المعتمد
./scripts/run_tests.sh                               # الاختبارات على البيئة المقفولة
```

قفل لم تُشغَّل عليه الاختبارات ليس مقفولًا — هو مجرد ملف.

**الإصدارات مثبَّتة بدقة في كل مكان:** Python 3.11.9 · postgres:16.4-bookworm ·
nginx:1.27.4-alpine · osrm-backend:v5.27.1. لا `latest` ولا وسم متحرك.

---

## ٣. الأسرار في هذه الحزمة

**لا يحتوي هذا الأرشيف على أي كلمة مرور أو مفتاح تشفير أو سر JWT.** المستبعَد
عمدًا:

- `.env.dev` — ملف البيئة المحلي (يحوي مفاتيح تشفير حقيقية). البديل المرفق:
  `.env.example` بقيم فارغة وأوامر توليد لكل واحدة.
- `var/` كاملًا — النسخ الاحتياطية، المستندات المخزَّنة، السجلات، أرشيف WAL،
  اللقطات، الملفات المرفوعة.
- أي مقابل قاعدة بيانات يحوي صفوفًا. المرفق `db/schema.sql` **بنية فقط**.

**فحص التصدير: بلا استثناء لأي ملف.** كل ملف نصي يدخل الأرشيف يُفحص —
`docs/` و`README.md` و`STATUS.md` و`.env.example` وملفات الاختبار وملف
الماسح نفسه. الاستثناء الوحيد على مستوى **القيمة**: نائب معلن من
`CHANGE_ME` أو `EXAMPLE_ONLY` أو `GENERATE_AT_DEPLOYMENT` (تطابق كامل، لا
يكفي الاحتواء)، أو توسيعة بيئة (`${VAR}` / `$(cmd)`). يُمنع التصدير عند وجود
كلمة مرور فعلية أو JWT أو مفتاح API أو مفتاح خاص أو رابط اتصال ببيانات
اعتماد. **عشرة اختبارات آلية** تزرع أسرارًا في `docs/` و`.env.example` و
`README.md` وتتحقق أن البناء يتوقف، ثم تتحقق أن التصدير النظيف ينجح.

**ولا كلمة مرور مثبّتة في الكود:**

- `scripts/seed.py` يولّد كلمة مرور عشوائية ويطبعها مرة واحدة، أو يأخذها من
  `MASAR_SEED_PASSWORD`.
- `scripts/run_tests.sh` يولّد واحدة لكل تشغيل ويمررها للقاعدة وللاختبارات عبر
  البيئة.
- `tests/support.py` بلا قيمة احتياطية — يفشل برسالة واضحة إن لم تُضبط.

---

## ٤. عيوب اكتُشفت وأُصلحت أثناء التحقق

مذكورة لأن معرفة **كيف** تُكتشف العيوب أنفع من قائمة نجاحات:

| العيب | كيف كان يختبئ | الأثر لو بقي |
|---|---|---|
| اختبار سلامة سجل التدقيق لم يُنفَّذ ولا مرة | يقرأ عبر RLS بلا سياق جلسة فيرى صفر صفوف، فيتخطّى بحجة «السجل فارغ» | «سلامة سجل التدقيق مثبتة» ادّعاء بلا اختبار |
| ولو نُفِّذ لنجح لسبب خاطئ | `id` نوعه `bigint` والمقارنة `::uuid` ترمي خطأ نوع يبتلعه `assertRaises(Exception)` | شهادة زور: يثبت فشل الاستعلام لا منع العبث |
| السائق لا يفتح رحلته بعد بدئها | `setInterval` يعيد رقمًا، وإسناد خاصية إليه يرمي `TypeError` قبل رسم الشاشة | شاشة فارغة بلا رسالة — وكل اختبارات HTTP تمر |
| `/driver.html` يعيد واجهة المكتب | الالتقاط الشامل يبتلع اسم الملف | تطبيق مختلف بلا أي إشعار |
| لا بوابة تمنع اعتماد خطة تقديرية | تحذير معروض والاعتماد مسموح بنقرة | التزام بـSLA على أرقام غير حقيقية |
| طلب OSRM غير مقسَّم ينهار بـ`414` | رابط ٩٩٠ عقدة = ١٩٫٣ ك.ب فوق حد nginx (٨ ك.ب) | يعمل في كل اختبار صغير، وينهار عند أول خطة وطنية |
| تصادم مواعيد بين خطتي الاختبار | يظهر فقط بين ٠٥:٠٠ و١٣:٠٠ — أي في أوقات العمل | ثلث سيناريوهات التنفيذ تُتخطّى لسبب لا علاقة له بها |
| سباق أرشفة WAL | الاختبار يبدأ قبل أرشفة أول مقطع | فشل عشوائي في سيناريو النسخ والاستعادة |

---

## ٥. ما يلزم قبل الإنتاج

بالترتيب:

1. **مزوّد طرق حقيقي** — `./deploy/osrm/activate.sh`. إلزامي: النظام يرفض
   اعتماد الخطط بدونه.
2. **خزنة أسرار** — المفاتيح الآن في ملف بيئة. فقدان مفتاح = فقدان ما شُفّر به
   نهائيًا، فاحفظ الحلقة بنسخة **مستقلة** عن نسخة القاعدة.
3. **توطين الاستضافة** — قرار تنظيمي، لا يمكن تأجيله إلى ما بعد الإطلاق.
4. **اختبار اختراق مستقل** — بعد استقرار الشكل النهائي.
5. **حساسات حرارة فعلية** — لأي عينات تتطلب تبريدًا.
